<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base theme -->
    <style name="Base.Theme.NextClass" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorSecondary">@color/secondary</item>

        <!-- Typography -->
        <item name="android:fontFamily">@font/poppins</item>
    </style>

    <!-- App theme inheriting from Base -->
    <style name="Theme.NextClass" parent="Base.Theme.NextClass" />
</resources>
